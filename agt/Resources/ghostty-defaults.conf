# agt built-in terminal defaults, loaded before ~/.config/ghostty/config so the
# user's own config still overrides them. widens libghostty's default 2px inner
# padding to 8px horizontally and 6px vertically.
window-padding-x = 8
window-padding-y = 6
