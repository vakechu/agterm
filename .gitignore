# downloaded prebuilt libghostty (never committed — see scripts/setup.sh)
GhosttyKit.xcframework/
agt/Resources/ghostty/
agt/Resources/terminfo/
*.tar.gz

# build artifacts
build/
.build/
agt.xcodeproj/
.swiftpm/

.DS_Store

# app state / runtime files (only appear if AGT_STATE_DIR is pointed at the repo)
settings.json
windows.json
workspaces.json
windows/
*.sock
